[project]
name = "gods-eye"
version = "0.1.0"
description = "Phase 1 — YOLO Person Detection for Border Surveillance AI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ultralytics>=8.3.0",
    "opencv-python>=4.10.0",
]

[tool.uv]
package = false
